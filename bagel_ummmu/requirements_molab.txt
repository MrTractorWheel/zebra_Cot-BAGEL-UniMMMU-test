# Python deps for Bagel-Zebra-CoT sampling on molab (Python 3.13, torch preinstalled).
#
# Notes:
# - torch/torchvision/numpy come preinstalled in the molab container (torch 2.11 + CUDA 13,
#   numpy 2.x) and are Blackwell-ready; we do NOT pin or reinstall them.
#   numpy 1.x / decord / pyarrow from the original Bagel requirements are NOT needed for
#   inference and have no Python 3.13 wheels - deliberately excluded.
transformers==4.49.0
accelerate>=0.34.0
safetensors>=0.4.5
huggingface_hub
hf_transfer
einops>=0.8.0
scipy
opencv-python-headless
PyYAML
requests
sentencepiece
tqdm
pandas
